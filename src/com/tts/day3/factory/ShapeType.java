package com.tts.day3.factory;

// We'll specify the shapes that we want our ShapeFactory (a single class responsible for instantiating
// different types of Shape objects) to be able to create:
public enum ShapeType {
	CIRCLE,
	RECTANGLE,
	SQUARE
}
